import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  metadataBase: new URL("https://mdx.vision"),
  title: "MDX Vision | AI-Powered Healthcare Documentation & AR Clinical Workflow",
  description:
    "Transform clinical workflows with MDX Vision's AI-powered voice documentation, real-time translation in 500+ languages, and augmented reality visualization.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
